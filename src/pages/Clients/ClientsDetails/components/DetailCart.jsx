export default function ClientDetailCart({ text, title }) {


    return (
        <>
            <div className="detail_cart">
                <p className="text">{text}</p>
                <h1 className="title">{title}</h1>
            </div>
        </>
    )
}